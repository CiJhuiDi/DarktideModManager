# 更新历史（Changelog）

项目主页：https://github.com/CiJhuiDi/DarktideModManager

> **新版本发布格式模板（2026-08-14 定）**
> - 版本段标题：`## vX.Y.Z（YYYY-MM-DD）`，日期为发布日
> - 条目以 `- ` 开头，一条一行；重要修复可用 `**加粗小标题**` 起头
> - GitHub Release 标题：`vX.Y.Z Beta`（**不加「版」字**，不叫「内测版」）
> - 发布时正文直接从本文件对应段落同步，用 `python tools/sync_release_notes.py vX.Y.Z`

## 待定（未发布）

**崩溃检测与控制台日志**
- 游戏退出后自动检测崩溃（管理器启动进程以 NTSTATUS 异常码退出 + crash_dumps 出现新文件，双信号），弹窗引导排查
- 崩溃弹窗显示最新控制台日志文件名；「查看控制台日志」一键打开 console_logs 目录（文本日志，记事本即可看 mod 报错）
- 关于页新增「📂 打开控制台日志文件夹」按钮，直达 %APPDATA%\Fatshark\Darktide\console_logs
- 控制台日志与崩溃转储同会话关联（文件名共享时间戳+GUID）

**游戏目录设置优化**
- 关于页新增「🔍 自动识别」按钮：扫描常见 Steam 库自动定位游戏目录并保存
- 游戏目录保存改为即时生效（热切换全局变量，不再要求重启应用）
- 自动识别找不到游戏目录时如实提示「未找到」，不再回退到写死的 D:\SteamLibrary 路径
- 游戏运行中禁止切换游戏目录（防呆守卫）；目录切换写入加锁原子化，避免并发读错乱

**其他**
- 搜索框新增一键清空按钮
- 「模拟游戏运行」新增「💥 模拟游戏崩溃」按钮（测试崩溃弹窗流程用）

## v0.3.1（2026-08-15）

**主题切换（7 主题 + 渐变 + 自定义背景）**
- 7 套主题：深渊暗潮（默认）/ 纯净黎明（浅色）/ 欢愉紫 / 疫病绿 / 愤怒红 / 神秘蓝 / 帝皇金，一键切换并持久化
- 静态渐变背景（每主题三段色）+ 面板微透明毛玻璃；4 种渐变方向（对角 / 水平 / 垂直 / 径向）
- 浅色主题适配：选中 / 高亮 / 悬浮 / 滚动条等 10+ 处硬编码色改为主题变量，避免黑白高对比刺眼
- 🎲 立即随机换主题；「每次启动时随机主题」复选框（勾选后每次启动自动换新主题）
- 🖼 自定义主题：导入图片（jpg/png/webp/bmp ≤8MB）作背景，指定亮 / 暗模式，可移除
- 启动零闪烁：主题在服务端直接内联进页面，打开即是正确主题，不再闪默认色
- 关于页布局自适应：卡片 / 输入框随窗口宽度伸缩，窄窗口按钮不换行

**备份空间保护**
- 备份保留策略：整合包 / DMF 组件备份各保留最近 10 份，超量自动清理最旧
- backups 目录总大小超 5GB 时从最旧开始清理，防止反复导入大整合包撑爆硬盘
- 清理动作在导入 / 恢复 / DMF 安装后自动执行，并提示清理数量

## v0.3.0（2026-08-15）

> **版本说明**：从 v0.2.3 跨 minor 升级到 v0.3.0（非 v0.2.4）——本次为 0.2.x 系列以来最大更新（6 项新功能 + 大量修复，+1500 行代码），按语义化版本规范新增功能应升 minor，且对玩家有「大更新」辨识度。

**游戏运行防呆**
- 游戏运行时自动锁定 mod 增删改：导入 / 删除 / 启停开关 / 保存 / 右键删除 / 拖拽导入全部变灰禁用
- 后端写操作 API 统一守卫（9 个接口游戏运行时拒绝），前端禁用 + 后端拦截双保险
- 新增「模拟游戏运行」开关（关于页，测试专用）：不开真游戏即可验证防呆逻辑，真实游戏运行时不可模拟为已退出

**覆盖更新 DMF**
- 顶部横幅新增「🔄 重新安装框架」：DMF 已装时也能强制用内置组件覆盖（旧文件自动备份，可在「备份」页找回）
- 后端 /api/dmf/install 支持 force 参数

**批量操作**
- 右键菜单新增「☑ 多选模式」：点击 mod 行多选，批量启用 / 禁用 / 删除 / 清残留

**导出与导入增强**
- 工具栏合并为「＋ 导入」（自动识别单 mod / 整合包 / 清单 txt），新增「导入文件夹」
- 导出支持三种模式：全部导出 / 按启用导出 / 仅导出清单（干净的 mod_load_order.txt）
- 新增导入清单：替换启停清单（旧清单自动备份，备份页可恢复）
- 导入识别防呆：mods/ 下单 mod 无清单时弹窗确认，避免误判

**依赖检查**
- 解析 .mod 的 packages 依赖：缺依赖（红）、循环依赖（橙）徽标提示
- 加载顺序检查：扩展 mod 应排在本体之后（如计分板扩展），顺序异常蓝色徽标提示
- 修复：资源路径 packages（content/wwise 等）误报为缺依赖

**差异对比**
- 导入整合包 / 恢复整合包备份前：预览新增 / 移除 / 更新 / 相同（结构化彩色列表）
- 恢复清单备份 / 应用预设前：预览将启用 / 将禁用变化

**备份页优化**
- 清单备份纳入备份列表，可恢复 / 删除
- 卡片精简摘要（启用了 N 个 mod，共 M 个），时间显示改为「2026-08-14 23:49」格式

**其他修复**
- 修复：关于页「🌐 GitHub」按钮、备份读取 422、tasklist GBK 解码崩溃
- 侧边栏底部版本信息两行居中显示

## v0.2.3（2026-08-14）

- 修复：因偷懒产生的荒诞错误
- 修复：关于页「🌐 GitHub」按钮点击无反应（事件绑定时机错误，按钮动态生成后未绑定监听）
- 版本号 v0.2.2 → v0.2.3，同步更新界面、exe 属性、随包文档

## v0.2.2（2026-08-14）

- 各类说明文档全面添加项目主页
- 关于页新增「🌐 GitHub」入口，一键打开项目主页

## v0.2.1（2026-08-13）

**根目录整洁化（A+B 方案）**

- 整合包导入不再拷贝根目录的说明文件/脚本（.bat/.txt/.md）——打补丁与启动游戏已由管理器接管，反复装包不再堆积重复文件
- 替换导入时自动把游戏根目录已有的散文件归档到 `backups/root_cleanup_*`（可恢复；`mod_load_order.txt` 参考副本与 `steam_appid.txt` 除外）
- 文档同步：不再推荐使用 toggle bat，统一走管理器

## v0.2.0（2026-08-13）

**删除 mod 文件**

- 右键菜单新增「🗑 删除 mod 文件」：文件夹移入回收站（可找回）+ 从启停清单移除 + 清除备注，一步到位
- 系统组件 base/dmf 禁止删除；与「从清单移除（清理残留）」功能互不冲突

## v0.1.9（2026-08-13）

**修复：右键打开文件夹失效（重要）**

- 根因：右键菜单项点击时目标 mod 名被"关闭菜单"逻辑提前清空，请求发出 `null` 导致误报"文件夹不存在"
- 菜单项改为先捕获目标再操作，并加严格判空；后端兜底拒绝无效名

## v0.1.8（2026-08-13）

**残留 mod 处理**

- 文件夹已不存在的残留 mod：右键「打开文件夹」自动禁用
- 新增「🗑 从清单移除（清理残留）」：一键清掉启停清单里的死条目

## v0.1.7（2026-08-13）

**右键菜单备注**

- 右键「✏️ 备注…」给 mod 加备注，显示为 `Mod名(备注)`，留空保存即清除
- 备注持久化在 `notes.json`（exe 旁）

## v0.1.6（2026-08-13）

**右键菜单**

- mod 行右键弹出自定义菜单：📂 打开文件夹 / 📋 复制 mod 名
- 菜单贴边定位、点击外部自动关闭

## v0.1.5（2026-08-13）

**中文显示名**

- 从 mod 本地化文件读取显示名（`mod_name`/`display_name`，优先中文），列表不再全是英文文件夹名
- 英文原名 + 版本号作为副标题；搜索同时匹配中英文；自动清理名字里的颜色代码/图标字符

## v0.1.4（2026-08-13）

**修复：卸载补丁后 DMF 误报**

- 自动装载插件被禁用（.off）不再被当作"组件缺失"，横幅显示正常提示而非红色报错

## v0.1.3（2026-08-13）

**修复：启动游戏后界面变空白（重要）**

- 根因：侧边栏导航误把「启动游戏」按钮当菜单按钮，隐藏全部页面后因缺少目标报错，导致内容区空白
- 导航只绑定真正的菜单按钮；回滚 v0.1.2 的白屏自愈改动（非根因）

## v0.1.2（2026-08-13）

**白屏自愈实验（已回滚）**

- 尝试对空白页做自动恢复处理，经 v0.1.3 定位非根因后整体回滚，无残留功能

## v0.1.1（2026-08-13）

**修复：搜索/筛选后保存导致其余 mod 全被禁用（重要）**

- 保存改为数据驱动，被过滤隐藏的启用 mod 不再丢失

## v0.1.0（2026-08-13）

- 版本号改为 0.x：正式进入 Beta 测试、收集反馈阶段
