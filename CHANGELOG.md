# 更新历史（Changelog）

项目主页：https://github.com/CiJhuiDi/DarktideModManager

> **新版本发布格式模板（2026-08-14 定）**
> - 版本段标题：`## vX.Y.Z（YYYY-MM-DD）`，日期为发布日
> - 条目以 `- ` 开头，一条一行；重要修复可用 `**加粗小标题**` 起头
> - GitHub Release 标题：`vX.Y.Z Beta`（**不加「版」字**，不叫「内测版」）
> - 发布时正文直接从本文件对应段落同步，用 `python tools/sync_release_notes.py vX.Y.Z`

## 待定

**修复**
- 修复：再次启动时不再弹「已经在运行中」提示框——检测到已有实例直接聚焦现有窗口并静默退出（原实现聚焦后仍弹 MessageBox，打断操作）
- 修复：多开聚焦现有窗口失效——tasklist 输出是 bytes，进程名校验用 str 做 `in` 判断抛 TypeError 被外层 except 吞成 False，聚焦代码从未执行（函数永远 False）；改 bytes 比较
- 修复：聚焦唤起最小化窗口白屏（持续、无法交互）——WebView2 最小化后 GPU 合成器挂起，还原时未唤醒（已知问题）。三重修复：① 聚焦方式弃用 keybd_event 模拟 Alt（按键注入可能干扰 WebView2 输入流），改 AttachThreadInput（当前线程 attach 目标+前台线程后 SetForegroundWindow，零按键注入）；② SW_RESTORE 仅当窗口最小化时才调用（无条件还原会把最大化窗口打回普通大小，破坏布局）；③ 还原后 RedrawWindow 强制整窗重绘 + 窗口侧监听 restored 事件做 +1/-1px resize 往返强制合成器重新唤醒

**修复**
- 修复：启动后卡「连接中…」（前端永远加载不出 mod 列表）——SortableJS 走 jsdelivr CDN 同步外链，CDN 超时/被墙时 Sortable 未定义，脚本中断导致 load() 不执行、/api/mods 永不请求（日志只有 status 轮询、页面永远「连接中」）；现已本地化（static/vendor/sortablejs/）+ 后端挂载 /vendor 静态路由，彻底摆脱 CDN 依赖

**修复**
- 修复：mod 列表加载时间不稳定（时长时短）——每次 `/api/mods` 都重新扫盘（347 个 mod 的目录枚举 + 文件读取），耗时随系统缓存状态波动（游戏运行时磁盘争抢 / Defender 实时扫描 / 重启后缓存冷）；现磁盘元数据（显示名/描述/版本/依赖）加 30 秒缓存：目录 mtime 签名自动失效（增删 mod 即重建）+ 写操作显式失效（导入/删除/恢复备份/改备注），启停状态仍每次现算；实测冷扫 434ms → 缓存命中 0ms，游戏运行时刷新/轮询不再扫盘
- 优化：首屏加载进一步减负——① 启动骨架屏（"正在加载 Mod 列表…"动画）替代白屏，JS 就绪后移除（10s 超时兜底）；② `/api/mods` 不再返回 description（悬停浮层才用），新增 `/api/mods/{name}/detail` 悬停时懒加载（本地缓存 + 防重入），首屏数据 85KB→55KB
- 修复：悬停浮层描述加载逻辑——无描述的 mod（如 psych_ward）会一直显示"描述加载中…"（空描述被当成未加载完）；现空描述显示"（无描述）"灰色小字
- 修复：前端加载慢（列表/状态栏卡顿）——① `/api/status` 每次调用都全量 `scan_mods()`（前端每 10s 轮询一次，等于每 10s 白扫一遍 300+ mod），且 `total` 字段前端根本不用；改为轻量目录计数 ② `is_game_running()` 每次起 tasklist 子进程（~350ms），改 CreateToolhelp32Snapshot 进程快照 API（<5ms），异常回退 tasklist ③ 前端渲染 347 行 mod 用 DocumentFragment 批量插入 + 依赖徽标预构建索引（原来 render 时逐 mod 全表 filter，O(n²)）；实测 status 接口 511ms→60ms，mods+status 并行加载明显变快。另新增 `/api/perf`：前端首次启动上报各阶段耗时到 app.log，排查"启动慢"时直接看日志定位卡点
- 修复：关窗后立刻重启，WebView2 初始化干等 47 秒（旧实例 WebView2 子进程未退干净、锁着用户数据目录）——窗口关闭后主动清理本实例 msedgewebview2 子进程，锁立刻释放，快速重启秒开
- 修复：app.log 无限增长（前端每 10s 轮询状态都会写 access 日志，长期运行可达数百 MB）——改为轮转日志：单文件超 2MB 自动切换，保留最近 3 份（总量封顶约 8MB），旧日志自动清理
- 修复：关窗后短时间内立刻重启被误判为多开（弹「已经在运行中」）——命名互斥体句柄从不主动关闭，窗口关闭后进程退出有延迟（Python 清理 + WebView2 子进程收尾），期间互斥体仍被占用；现窗口关闭后主动释放互斥体，且新实例检测到占用时先聚焦已有窗口、窗口已消失则短暂等待旧进程退出后重试（真多开才拦截）
- 修复：关于页「选择文件夹」设置游戏目录失败（`{"detail":"Not Found"}`）——后端 `api_set_game_dir` 在架构拆分时丢失了 `@app.post("/api/game_dir")` 装饰器（路由从未注册，函数成死代码），且前端调用的 `/api/settings/game_dir` 与后端路径不一致；现补回装饰器、前端统一改为 `/api/game_dir`，并在 test_api 新增路由回归用例
- 修复：启动加载慢（玩家反馈"每次启动等半天"，机械盘 + 大量 mod 时尤甚）——启动时 `/api/mods` 与 `/api/deps/check` 连续两次全量扫描，每个 mod 都用 rglob 递归遍历整个目录树找 localization 文件且第二遍白跑（缓存检查在搜索之后）；现 localization 搜索改为深度受限遍历（4 层内）+ 完整缓存（含文件列表，命中后仅 stat 少量文件）、`scan_mods` 单次读取显示名/描述、`.mod` 文件只读一次（version + packages 共用内容）、依赖解析加 mtime 缓存；SSD 实测单次扫描 0.75s→0.40s、连续第二次 0.16s（原来二次扫描仍全树遍历），机械盘上收益更大
- 修复：搜索/筛选生效时启用某个停用 mod，保存后会被错误排到清单第一位——collectEnabledOrder 在过滤视图下按可见 DOM 行收集顺序，而过滤只显示匹配子集，顺序≠完整清单顺序；现过滤生效时完全按数据顺序（清单原序）收集，仅无过滤时用 DOM 顺序（拖拽结果）

**语录显示（暗潮加载画面语录）**
- 底部**状态栏内嵌语录胶囊**（水平居中，不再单开栏位）：显示暗潮加载画面官方语录（310 条，取自游戏 loading_view_settings.lua，2026-08-19 导出），超长省略、悬停看全文
- **有效操作才换**：点击可交互元素（按钮 / 启停开关 / 标签 / 横幅 / mod 行 / 右键菜单 / 日志视图等，200ms 同元素去重）+ 拖拽排序后自动换一条；点空白/装饰区等**空操作不换**；一轮播完重新洗牌不重复
- 点击语录胶囊可手动换一条

## v0.4.1（2026-08-17）

**行为变更**
- 停用 mod 不再写入清单注释行（`--ModName`）：mod_load_order.txt 只保留**启用中的 mod**与说明注释，清单保持干净；旧式禁用标记在保存/导入/批量操作时自动清理。注意：停用后再启用会排到清单末尾（顺序不保留，暗潮对加载顺序敏感的 mod 请用排序功能调整）

**修复**
- 修复：mod 只能启用不能停用——保存时「被过滤隐藏的启用 mod」逻辑（collectEnabledOrder 第 2 步）读的是渲染时的数据快照，取消勾选后 checkbox 只改 DOM 不同步数据，被停用的 mod 又被加回启用列表；现 checkbox 变更同步进 mods 数据，停用/启用/过滤场景均正确
- 修复：游戏关闭后 mod 启停开关仍保持禁用（启动游戏按钮已恢复，但 checkbox 仍灰锁，无法停用/启用 mod）——轮询更新游戏运行状态后只刷新了部分按钮，mod 列表开关的禁用态在渲染时写死不随状态同步；现改为 `updateEditLocks` 直接同步列表开关禁用态，不依赖整表重渲染
- 修复：游戏运行中仍可拖拽排序 mod 行（保存会被后端守卫拒绝，体验不一致）——Sortable 拖拽在游戏运行中禁止（onMove 拦截），与其它写操作防呆一致
- 工具：新增 `tools/set_alpha_state.py`（正式态 vX.Y.Z → Alpha 测试态一键切换，与 build_release 的恢复逻辑镜像）；build_alpha 增加 stdout/stderr UTF-8 重配置，修复 GBK 控制台打印 emoji 崩溃

## v0.4.0（2026-08-16）

**崩溃检测与控制台日志**
- 游戏退出后自动检测崩溃（管理器启动进程以 NTSTATUS 异常码退出 + crash_dumps 出现新文件，双信号），弹窗引导排查
- 崩溃弹窗显示最新控制台日志文件名；「查看控制台日志」一键打开 console_logs 目录（文本日志，记事本即可看 mod 报错）
- 关于页新增「📂 打开控制台日志文件夹」按钮，直达 %APPDATA%\Fatshark\Darktide\console_logs
- 控制台日志与崩溃转储同会话关联（文件名共享时间戳+GUID）

**游戏目录设置优化**
- 关于页新增「🔍 自动识别」按钮：扫描常见 Steam 库自动定位游戏目录并保存
- 游戏目录保存改为即时生效（热切换全局变量，不再要求重启应用）
- 自动识别找不到游戏目录时如实提示「未找到」，不再回退到写死的 D:\SteamLibrary 路径
- 游戏运行中禁止切换游戏目录（防呆守卫）；目录切换写入加锁原子化，避免并发读错乱

**其他**
- 搜索框新增一键清空按钮
- 「模拟游戏运行」新增「💥 模拟游戏崩溃」按钮（测试崩溃弹窗流程用）

**架构重构与修复（v0.3.1 后）**
- 后端架构拆分：app.py 2443 → 1054 行，业务模块归入 core/ 包（state/load_order/patch/mods/imports/dmf/crash/theme/profiles）
- 修复：exe 打包后空白页（首页路由装饰器被迁移脚本吞掉）、frozen 模式路径错乱（BASE_DIR 取到 exe 文件本身）
- 新增 tools/start_test_env.py 一键测试环境（杀进程→清端口→建 mock→起服务）

**多开检测优化**
- 单实例锁前缀 Global\ → Local\（消除 SeCreateGlobalPrivilege 权限误判，防「打不开软件」）
- 多开被拒时自动聚焦已有实例窗口（校验窗口进程必须属于本程序，防误中资源管理器同名标签页）
- 多开尝试记录到 app.log

**实验模式：日志预览与崩溃报告导出（demo）**
- 关于页新增「🔬 实验：日志预览」：应用内直接查看最新控制台日志尾部，疑似错误行（Lua/Error/Exception 等）标红高亮
- 崩溃报告一键导出：打包最新控制台日志 + 崩溃转储 + 报告信息（发作者/群友用）

**日志分析增强（实验模式）**
- 侧边栏动态「🔬 日志分析」标签页：需要时才出现（关于页按钮/崩溃弹窗/拖拽 .log 触发），可关闭
- 多标签：最新日志常驻 + 每次导入旧日志自动新标签，标签带 ✕ 独立关闭
- 三分类过滤：错误 / 警告 / 通常按钮（互斥，再点取消）+ 关键词搜索叠加
- 行颜色：错误红、警告黄；「复制选中文本/复制此行/复制选中行」右键菜单
- 多选模式：点击/拖动批量选中（实时预览、反色高亮），提示条常显；禁用拖选防误触
- 旧日志导入：按钮选择或**拖拽 .log 文件进窗口**（全局拖放，游戏运行中也允许；只读分析）
- 崩溃弹窗「查看控制台日志」改为打开日志分析页

**mod 悬停详细描述**
- 悬停 mod 行显示浮层：中文名/原名/版本/描述（localization mod_description，优先中文）/备注/依赖/缺失警告
- 去除原生 title 白框，统一自定义浮层

**横幅显示控制**
- 按钮伪下拉 + 复选框单独控制 DMF/补丁/依赖三个横幅显隐；一键收起/展开全部（按钮文字随状态实时同步）
- 收起的横幅显示为指示灯（圆点颜色=状态）带名称，│ 分隔
- 「自动收起正常横幅」：状态刚变正常自动收起、刚变异常自动展开提示；手动展开后 30 秒内不被自动收起；依赖横幅不参与自动收起（正常也显示「✓ 依赖检查正常」）
- 依赖横幅点击：逐个跳转并闪光定位问题 mod（缺依赖/循环/顺序，按列表顺序轮询）

**界面与稳定性修复**
- 修复：窗口最小化后坐标 -32000 被记忆，下次启动窗口在屏幕外（任务栏有图标无窗口）
- 修复：日志预览弹窗宽度不生效（wide/ultra 类加错元素）
- 前端组件模板化：docs/UI_TEMPLATE.md 统一组件库，新功能复用不重写样式

## v0.3.1（2026-08-15）

**主题切换（7 主题 + 渐变 + 自定义背景）**
- 7 套主题：深渊暗潮（默认）/ 纯净黎明（浅色）/ 欢愉紫 / 疫病绿 / 愤怒红 / 神秘蓝 / 帝皇金，一键切换并持久化
- 静态渐变背景（每主题三段色）+ 面板微透明毛玻璃；4 种渐变方向（对角 / 水平 / 垂直 / 径向）
- 浅色主题适配：选中 / 高亮 / 悬浮 / 滚动条等 10+ 处硬编码色改为主题变量，避免黑白高对比刺眼
- 🎲 立即随机换主题；「每次启动时随机主题」复选框（勾选后每次启动自动换新主题）
- 🖼 自定义主题：导入图片（jpg/png/webp/bmp ≤8MB）作背景，指定亮 / 暗模式，可移除
- 启动零闪烁：主题在服务端直接内联进页面，打开即是正确主题，不再闪默认色
- 关于页布局自适应：卡片 / 输入框随窗口宽度伸缩，窄窗口按钮不换行

**备份空间保护**
- 备份保留策略：整合包 / DMF 组件备份各保留最近 10 份，超量自动清理最旧
- backups 目录总大小超 5GB 时从最旧开始清理，防止反复导入大整合包撑爆硬盘
- 清理动作在导入 / 恢复 / DMF 安装后自动执行，并提示清理数量

## v0.3.0（2026-08-15）

> **版本说明**：从 v0.2.3 跨 minor 升级到 v0.3.0（非 v0.2.4）——本次为 0.2.x 系列以来最大更新（6 项新功能 + 大量修复，+1500 行代码），按语义化版本规范新增功能应升 minor，且对玩家有「大更新」辨识度。

**游戏运行防呆**
- 游戏运行时自动锁定 mod 增删改：导入 / 删除 / 启停开关 / 保存 / 右键删除 / 拖拽导入全部变灰禁用
- 后端写操作 API 统一守卫（9 个接口游戏运行时拒绝），前端禁用 + 后端拦截双保险
- 新增「模拟游戏运行」开关（关于页，测试专用）：不开真游戏即可验证防呆逻辑，真实游戏运行时不可模拟为已退出

**覆盖更新 DMF**
- 顶部横幅新增「🔄 重新安装框架」：DMF 已装时也能强制用内置组件覆盖（旧文件自动备份，可在「备份」页找回）
- 后端 /api/dmf/install 支持 force 参数

**批量操作**
- 右键菜单新增「☑ 多选模式」：点击 mod 行多选，批量启用 / 禁用 / 删除 / 清残留

**导出与导入增强**
- 工具栏合并为「＋ 导入」（自动识别单 mod / 整合包 / 清单 txt），新增「导入文件夹」
- 导出支持三种模式：全部导出 / 按启用导出 / 仅导出清单（干净的 mod_load_order.txt）
- 新增导入清单：替换启停清单（旧清单自动备份，备份页可恢复）
- 导入识别防呆：mods/ 下单 mod 无清单时弹窗确认，避免误判

**依赖检查**
- 解析 .mod 的 packages 依赖：缺依赖（红）、循环依赖（橙）徽标提示
- 加载顺序检查：扩展 mod 应排在本体之后（如计分板扩展），顺序异常蓝色徽标提示
- 修复：资源路径 packages（content/wwise 等）误报为缺依赖

**差异对比**
- 导入整合包 / 恢复整合包备份前：预览新增 / 移除 / 更新 / 相同（结构化彩色列表）
- 恢复清单备份 / 应用预设前：预览将启用 / 将禁用变化

**备份页优化**
- 清单备份纳入备份列表，可恢复 / 删除
- 卡片精简摘要（启用了 N 个 mod，共 M 个），时间显示改为「2026-08-14 23:49」格式

**其他修复**
- 修复：关于页「🌐 GitHub」按钮、备份读取 422、tasklist GBK 解码崩溃
- 侧边栏底部版本信息两行居中显示

## v0.2.3（2026-08-14）

- 修复：因偷懒产生的荒诞错误
- 修复：关于页「🌐 GitHub」按钮点击无反应（事件绑定时机错误，按钮动态生成后未绑定监听）
- 版本号 v0.2.2 → v0.2.3，同步更新界面、exe 属性、随包文档

## v0.2.2（2026-08-14）

- 各类说明文档全面添加项目主页
- 关于页新增「🌐 GitHub」入口，一键打开项目主页

## v0.2.1（2026-08-13）

**根目录整洁化（A+B 方案）**

- 整合包导入不再拷贝根目录的说明文件/脚本（.bat/.txt/.md）——打补丁与启动游戏已由管理器接管，反复装包不再堆积重复文件
- 替换导入时自动把游戏根目录已有的散文件归档到 `backups/root_cleanup_*`（可恢复；`mod_load_order.txt` 参考副本与 `steam_appid.txt` 除外）
- 文档同步：不再推荐使用 toggle bat，统一走管理器

## v0.2.0（2026-08-13）

**删除 mod 文件**

- 右键菜单新增「🗑 删除 mod 文件」：文件夹移入回收站（可找回）+ 从启停清单移除 + 清除备注，一步到位
- 系统组件 base/dmf 禁止删除；与「从清单移除（清理残留）」功能互不冲突

## v0.1.9（2026-08-13）

**修复：右键打开文件夹失效（重要）**

- 根因：右键菜单项点击时目标 mod 名被"关闭菜单"逻辑提前清空，请求发出 `null` 导致误报"文件夹不存在"
- 菜单项改为先捕获目标再操作，并加严格判空；后端兜底拒绝无效名

## v0.1.8（2026-08-13）

**残留 mod 处理**

- 文件夹已不存在的残留 mod：右键「打开文件夹」自动禁用
- 新增「🗑 从清单移除（清理残留）」：一键清掉启停清单里的死条目

## v0.1.7（2026-08-13）

**右键菜单备注**

- 右键「✏️ 备注…」给 mod 加备注，显示为 `Mod名(备注)`，留空保存即清除
- 备注持久化在 `notes.json`（exe 旁）

## v0.1.6（2026-08-13）

**右键菜单**

- mod 行右键弹出自定义菜单：📂 打开文件夹 / 📋 复制 mod 名
- 菜单贴边定位、点击外部自动关闭

## v0.1.5（2026-08-13）

**中文显示名**

- 从 mod 本地化文件读取显示名（`mod_name`/`display_name`，优先中文），列表不再全是英文文件夹名
- 英文原名 + 版本号作为副标题；搜索同时匹配中英文；自动清理名字里的颜色代码/图标字符

## v0.1.4（2026-08-13）

**修复：卸载补丁后 DMF 误报**

- 自动装载插件被禁用（.off）不再被当作"组件缺失"，横幅显示正常提示而非红色报错

## v0.1.3（2026-08-13）

**修复：启动游戏后界面变空白（重要）**

- 根因：侧边栏导航误把「启动游戏」按钮当菜单按钮，隐藏全部页面后因缺少目标报错，导致内容区空白
- 导航只绑定真正的菜单按钮；回滚 v0.1.2 的白屏自愈改动（非根因）

## v0.1.2（2026-08-13）

**白屏自愈实验（已回滚）**

- 尝试对空白页做自动恢复处理，经 v0.1.3 定位非根因后整体回滚，无残留功能

## v0.1.1（2026-08-13）

**修复：搜索/筛选后保存导致其余 mod 全被禁用（重要）**

- 保存改为数据驱动，被过滤隐藏的启用 mod 不再丢失

## v0.1.0（2026-08-13）

- 版本号改为 0.x：正式进入 Beta 测试、收集反馈阶段
